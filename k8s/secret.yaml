apiVersion: v1
kind: Secret
metadata:
  name: timesheet-app-secret
  namespace: timesheet-app
  labels:
    app.kubernetes.io/name: timesheet-app
    app.kubernetes.io/component: secret
type: Opaque
# Values should be supplied via Jenkins pipeline or sealed-secrets in production.
# The placeholders below are base64-encoded defaults for reference only.
# echo -n 'change-me-in-production' | base64
data:
  JWT_SECRET: "Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24="
  # Azure SQL credentials – override via Jenkins pipeline
  # AZURE_SQL_SERVER, AZURE_SQL_DATABASE, AZURE_SQL_USER, AZURE_SQL_PASSWORD
  # are injected at deploy time (see Jenkinsfile)
